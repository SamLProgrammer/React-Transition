import { render, renderHook } from "@testing-library/react"
import { useTodo } from "../../src/hooks/useTodo"

describe('Testing useTodo.js', () => { 
    test('should show component by default', () => { 
        // const {result} = renderHook(useTodo());
        // console.log(result);
     })
 })